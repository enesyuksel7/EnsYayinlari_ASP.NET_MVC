@model IEnumerable<EnsYayinlariMVC.Models.DenemeKayitlari>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutCikisYap.cshtml";
}

<div style="background-color:green">
    <p>
        <a class="btn btn-primary" href="@Url.Action("Admin","Uyeler")">Panele Dön</a>
        <a class="btn btn-dark" href="@Url.Action("Create","DenemeKayitlari")">Yeni Kayıt</a>
    </p>
    <table class="table text-light">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AdSoyad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EPosta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Yas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sinif)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Alan)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.AdSoyad)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Telefon)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EPosta)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Yas)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sinif)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Alan)
                </td>
                <td>
                    @Html.ActionLink("Düzenle", "Edit", new { id = item.Id }, new { @class = "btn btn-warning" }) |
                    @Html.ActionLink("Detaylar", "Details", new { id = item.Id }, new { @class = "btn btn-info" }) |
                    @Html.ActionLink("Sil", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" }) |
                </td>
            </tr>
        }

    </table>
</div>